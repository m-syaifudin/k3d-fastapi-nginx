FastAPI CRUD Application with Kubernetes Deployment

A simple CRUD (Create, Read, Update, Delete) REST API built with FastAPI, 
containerized using Docker, deployed to a Kubernetes cluster on a VPS, 
exposed through Nginx Ingress with a custom domain, and automated with GitHub Actions CI/CD.
